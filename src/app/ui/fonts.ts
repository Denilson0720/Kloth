import { Playfair } from "next/font/google";
import { Open_Sans } from "next/font/google";
// only for titles
export const playfair = Playfair({subsets:['latin']})
// opensans for main font
export const opensans = Open_Sans({subsets:['latin']})